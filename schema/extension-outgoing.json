{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "OutgoingExtensionMessage",
    "type": "object",
    "required": ["messageType", "options"],
    "properties": {
        "messageType": {
            "type": "string"
        },
        "id": {
            "description": "If the `id` field is present, it indicates that a response is needed",
            "type": "number"
        },
        "options": {
            "type": "object"
        }
    }
}
