{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "PhishingStatus",
    "type": "object",
    "description": "This describes the payload required to set the phishing status",
    "additionalProperties": false,
    "required": ["phishingStatus"],
    "properties": {
        "phishingStatus": {
            "description": "Set to true if page is potentially malicious",
            "type": "boolean"
        }
    }
}
