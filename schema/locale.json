{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "LocaleSettings",
    "type": "object",
    "description": "This describes the payload required to set the locale",
    "additionalProperties": false,
    "required": ["locale"],
    "properties": {
        "locale": {
            "description": "2 letters, such as `pl`, `en`, `fr` etc",
            "type": "string"
        }
    }
}
